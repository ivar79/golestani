# --- secrets (never commit) ---
.env
.env.*
!.env.example

# --- dependencies (reproducible via lockfiles) ---
node_modules/
vendor/

# --- OS / editor / tooling junk ---
_internal/
Thumbs.db
Desktop.ini
*.result.cache
.freebuff/

# --- local backup snapshots (never commit/push) ---
backup/
*.zip
api/public/storage

# --- huge local map archives (never commit/push to GitHub) ---
iran-260909.osm.pbf
iran-shortbread-1.0.mbtiles
iran-shortbread-*.mbtiles
iran-*.osm.pbf
*.mbtiles
*.pmtiles
*.osm.pbf
*.pbf

# --- local documentation drafts ---
*.md