"use client";
import { useState } from "react";
import Link from "next/link";
import { searchBusinesses, type Business } from "@/lib/businesses";
import AppTaskbar from "@/components/layout/AppTaskbar";
export default function SearchPage(){const [q,setQ]=useState("");const [city,setCity]=useState("");const [items,setItems]=useState<Business[]>([]);const [loading,setLoading]=useState(false);async function submit(e:React.FormEvent){e.preventDefault();setLoading(true);try{const result=await searchBusinesses({q:q||undefined,city:city||undefined,limit:20});setItems(result.data??result as unknown as Business[])}finally{setLoading(false)}}return <main dir="rtl" className="min-h-screen bg-night px-4 pb-10 pt-36 text-white">
  <AppTaskbar /><div className="mx-auto max-w-7xl"><h1 className="text-3xl font-black">جست‌وجوی کسب‌وکار</h1><form onSubmit={submit} className="mt-6 grid gap-3 rounded-2xl border border-white/10 bg-white/5 p-4 md:grid-cols-[1fr_220px_auto]"><input value={q} onChange={e=>setQ(e.target.value)} placeholder="نام، خدمت یا دسته‌بندی" className="rounded-xl bg-white/10 p-4 outline-none"/><input value={city} onChange={e=>setCity(e.target.value)} placeholder="شهر" className="rounded-xl bg-white/10 p-4 outline-none"/><button className="btn btn-primary px-6 font-bold">{loading?"در حال جست‌وجو…":"جست‌وجو"}</button></form><div className="mt-8 grid gap-6 lg:grid-cols-[1fr_360px]"><section className="grid gap-4">{items.map(b=><article key={b.id} className="rounded-2xl border border-white/10 bg-white/5 p-5"><div className="flex justify-between gap-4"><div><p className="text-sm text-secondary">{b.category??"کسب‌وکار محلی"}</p><h2 className="mt-2 text-xl font-bold">{b.name}</h2><p className="mt-2 text-sm text-surface-variant">{[b.city,b.neighborhood,b.address].filter(Boolean).join("، ")}</p></div>{b.distance&&<span className="text-sm text-surface-variant">{Math.round(b.distance)} متر</span>}</div><div className="mt-4 flex gap-3"><Link href={`/b/${b.slug}`} className="btn btn-primary btn-sm">مشاهده</Link>{b.phone&&<a href={`tel:${b.phone}`} className="btn btn-secondary btn-sm">تماس</a>}</div></article>)}</section><aside className="min-h-64 rounded-2xl border border-white/10 bg-white/5 p-5"><h2 className="font-bold">موقعیت نتایج</h2><p className="mt-4 text-sm leading-7 text-surface-variant">نتایج مکانی بر اساس مختصات ثبت‌شده و فاصله محاسبه‌شده نمایش داده می‌شوند.</p></aside></div></div></main>}
